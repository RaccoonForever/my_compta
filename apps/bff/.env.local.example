# BFF – Environment variables for local development
# Copy this file to .env.local and fill in the values.

# Firebase project (used by emulators in local mode)
FIREBASE_PROJECT_ID=my-compta-dev

# Emulator hosts (pre-configured for Docker Compose networking)
FIRESTORE_EMULATOR_HOST=firebase:8080
FIREBASE_AUTH_EMULATOR_HOST=firebase:9099

# API
PORT=4001
API_RATE_LIMIT_REQS_PER_MIN=120
NODE_ENV=development
