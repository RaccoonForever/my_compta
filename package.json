{
  "name": "my-compta",
  "version": "0.1.0",
  "private": true,
  "description": "Personal cash-flow accountability app",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "docker compose up",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:integration": "pnpm -r test:integration"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
