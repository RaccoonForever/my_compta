# Frontend (React + Vite)

Prerequisites: Node 18+ and npm or yarn.

Install and run (from `frontend/`):

```bash
npm install
npm run dev
```

This is a minimal React + Vite scaffold to call the backend API.
